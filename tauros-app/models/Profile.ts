export class Profile {
  username: string;
  bio: string;

  constructor(username: string, bio: string) {
    this.username = username;
    this.bio = bio;
  }

  // Additional methods to manage profile data
} 